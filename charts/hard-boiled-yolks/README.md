# hard-boiled-yolks Helm library

Shared resource templates for Hard Boiled Yolks application charts. Consumers alias this dependency as `hby` and configure image, controller, storage, security, ports, ExternalSecrets, Gateway API routes, scheduling, and workload extensions under their `hby:` values key.

This is a Helm library chart and does not render resources when installed directly.
